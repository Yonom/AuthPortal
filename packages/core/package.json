{
  "name": "@authportal/core",
  "version": "0.0.0",
  "scripts": {
    "predeploy": "npm run build && cd dist",
    "deploy": "cd dist && npm pack && npm publish --access public",
    "build": "tsc",
    "postbuild": "cp package.json dist/package.json"
  }
}
